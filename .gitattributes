# Archive files
*.tar.gz filter=lfs diff=lfs merge=lfs -text
*.zip filter=lfs diff=lfs merge=lfs -text

# Specific paths for optimized backups
backups/**/*.tar.gz filter=lfs diff=lfs merge=lfs -text
backups/**/*.zip filter=lfs diff=lfs merge=lfs -text

# Documentation files (not LFS)
*.md text diff=markdown
*.txt text
*.json text
*.yaml text
*.yml text

# Python files (not LFS)
*.py text diff=python
*.pyw text diff=python
*.pyx text diff=python
*.pxd text diff=python

# Shell scripts (not LFS)
*.sh text eol=lf
*.bash text eol=lf
*.zsh text eol=lf

# Keep these files in Git, not LFS
!backups/**/*.md
!backups/**/*.txt
!backups/**/*.json
!backups/**/*.yaml
!backups/**/*.yml
