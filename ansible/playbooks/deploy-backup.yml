---
# Backup Solutions Deployment
# Deploys backup and disaster recovery solutions

- name: Deploy Backup Solutions
  debug:
    msg: "💾 Deploying backup and disaster recovery solutions..."

- name: Create backup namespace
  shell: kubectl create namespace backup --dry-run=client -o yaml | kubectl apply -f -
  environment:
    KUBECONFIG: /etc/rancher/k3s/k3s.yaml

- name: Display backup deployment status
  debug:
    msg: "✅ Backup solutions deployment initiated successfully!"
