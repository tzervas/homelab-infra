# Unified Domain Configuration
# Single source of truth for all domain-related settings
#
# THIS FILE IS AUTOMATICALLY GENERATED - DO NOT EDIT DIRECTLY
# Generated at: 2025-07-30T20:47:56.852764Z
#
# To modify domain settings, update:
#   - .env file for environment-specific domains
#   - config/consolidated/services.yaml for service list
#   - Then run: scripts/generate-domains-config.py

domains:
  base:
    primary: homelab.local
    tld: local
global:
  domain:
    base: homelab.local
    tld: local
  labels:
    app.kubernetes.io/managed-by: homelab-infra
    app.kubernetes.io/part-of: homelab
    homelab.local/environment: "{{ .Values.environment }}"
    homelab.local/component: "{{ .Values.component }}"
  services:
    metallb: metallb.homelab.local
    cert_manager: cert_manager.homelab.local
    ingress_nginx: ingress_nginx.homelab.local
    longhorn: longhorn.homelab.local
    monitoring: monitoring.homelab.local
    gitlab: gitlab.homelab.local
    keycloak: keycloak.homelab.local
    jupyter: jupyter.homelab.local
    ollama: ollama.homelab.local
    external: external.homelab.local
    internal: internal.homelab.local
    infrastructure: infrastructure.homelab.local
    applications: applications.homelab.local
    prometheus: prometheus.homelab.local
    grafana: grafana.homelab.local
    loki: loki.homelab.local
    service_monitors: service_monitors.homelab.local
    log_collection: log_collection.homelab.local
    auth: auth.homelab.local
    homelab: homelab.local
  environments:
    development:
      suffix: development
      base: development.homelab.local
    staging:
      suffix: staging
      base: staging.homelab.local
    production:
      suffix: ""
      base: homelab.local
dns:
  cluster_dns: 10.43.0.10
  search_domains:
    - homelab.local
    - svc.cluster.local
    - cluster.local
certificates:
  issuer:
    email: admin@homelab.local
    staging_server: https://acme-staging-v02.api.letsencrypt.org/directory
    production_server: https://acme-v02.api.letsencrypt.org/directory
  defaults:
    duration: 2160h
    renewBefore: 360h
    algorithm: RSA
    size: 2048
  ca:
    common_name: Homelab CA
    organization: Homelab
    organizational_unit: Homelab Infrastructure
    country: US
    province: Home
    locality: Lab

# Image Registry Settings (from global.yaml)
registry:
  default: "docker.io"
  pullPolicy: "IfNotPresent"
  pullSecrets: []

# Common Annotations
annotations:
  deployment.kubernetes.io/revision: '{{ .Values.revision | default "1" }}'
  homelab.local/managed-by: "centralized-config"
  homelab.local/last-updated: '{{ now | date "2006-01-02T15:04:05Z" }}'
