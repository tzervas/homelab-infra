# Development Environment Secrets (Plain)
# TLS Certificate Configuration
tls:
  issuer: "letsencrypt-staging"

# Basic secrets for development
secrets:
  grafana:
    adminPassword: "devadmin"

  database:
    adminPassword: "devadmin123"
    userPassword: "devuser123"
