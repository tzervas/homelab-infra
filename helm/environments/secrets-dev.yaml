# Development Environment Secrets
# Empty secrets file for development environment
# This prevents helmfile from failing when secrets are referenced but not needed

environment: development

# Placeholder secrets for development - use plain text for dev
secrets:
  example_secret: "dev-placeholder-value"
