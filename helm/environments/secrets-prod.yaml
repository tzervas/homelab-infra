environment: production

# Production Environment Secrets
# IMPORTANT: Use sealed-secrets or external-secrets for production
# Never store plain text secrets in production
secrets:
  # Example structure - replace with actual sealed secrets
  example_secret: "REPLACE_WITH_SEALED_SECRET"
